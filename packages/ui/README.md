# Shared UI

Audited All Rise visual tokens and dependency-free React primitives. Components use semantic
HTML and class names whose implementation lives beside the web shell, keeping shared components
portable without duplicating application state or analytics.
