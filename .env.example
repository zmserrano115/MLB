# Hosted reads use Turso. Keep credentials server-side and never commit values.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
TURSO_READ_ONLY=true
TURSO_DATA_VERSION=

# Optional local override. Defaults to data/mlb.db.
MLB_DB_PATH=data/mlb.db
